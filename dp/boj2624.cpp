#include<bits/stdc++.h>
using namespace std;

int t, k, a[10001];

int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    cin >> t >> k;
    for (int i = 0; i < k; i++){
        int coin, num;
        cin >> coin >> num;
        a[coin] = num;
    }
    
    for (int i = 0; i <= t; i++){
        if(a[i]){
            
        }
    }
    
}